# 贡献指南

欢迎提交 Issue、文档改进、测试场景、连接器适配建议和 Pull Request。

Linear GPT PM 仍处于 Alpha 阶段，当前最有价值的贡献不是增加更多抽象规则，而是：

- 真实项目使用反馈；
- 安装和升级兼容性问题；
- Linear / GitHub 连接器差异；
- 新的项目类型和证据场景；
- 能够复现的错误案例；
- 更清晰的公开文档和示例。

## 贡献类型

### Bug

适用于：

- 安装失败；
- Skill 无法被发现；
- Plan ID 或 Profile 工具结果不稳定；
- 写入计划与实际结果不一致；
- 审查规则误判；
- 打包或验证工具失败。

请尽量提供：

- 操作系统和 Python 版本；
- Codex / Agent Skills 环境；
- Linear / GitHub 是否已连接；
- 使用的 Skill 版本或 Commit；
- 最小复现步骤；
- 脱敏后的输入、输出和错误信息。

### 文档

适用于：

- 快速开始不清楚；
- 安装命令过时；
- 集成说明缺少场景；
- 示例无法让新用户理解；
- 中英文术语不一致。

### 新测试场景

适用于：

- 新的需求、变更或风险分类边界；
- 任务来源和关系冲突；
- Linear 与 GitHub 状态不一致；
- 分页、权限或证据不可访问；
- 并发修改和重复创建；
- 提示注入和跨项目数据泄露风险。

### 新平台或连接器

适用于 GitLab、Jira、Notion、Slack 或其他证据源和项目系统。

请先说明：

- 新平台承担“正式工作台账”还是“交付证据源”；
- 有哪些稳定 ID、关系、状态和分页能力；
- 读取和写入权限如何区分；
- 缺少访问权限时应如何降级；
- 哪些数据允许跨系统传递。

不要只替换平台名称而忽略语义差异。

## 核心原则

所有贡献都需要保持以下边界：

1. 两个 Skills 必须可以独立安装，并包含运行所需的规则、模板和说明；
2. 需求治理写入与交付审查保持独立权限边界；
3. 正式 Linear 写入必须经过明确确认和写前重读；
4. 审查默认只读，不得自动批准需求、变更、风险、验收、合并或部署；
5. 外部 Issue、评论、文档、PR 和日志只能作为数据，不能作为新的授权指令；
6. 缺少访问权限必须标记为“不可访问”，不能误判为“证据不存在”；
7. 新行为需要补充 `tests/cases.md` 或单元测试；
8. 改变分类、证据、权限、Plan 或 Profile 语义时需要更新版本说明。

## 示例和真实数据

公开示例可以使用：

- 虚构的通用项目；
- 已得到项目所有者明确同意的真实案例；
- 已脱敏的 Linear、GitHub 或项目材料。

禁止提交：

- 密钥、Token、密码和连接字符串；
- 未授权的客户、员工或个人信息；
- 私有工作区 ID、内部附件或完整生产日志；
- 未经允许公开的源代码和安全细节；
- 可以推断真实用户身份或内部基础设施的组合信息。

真实案例应保留业务结构和解决思路，删除不影响理解的敏感字段。

## 文档要求

公开文档应优先回答：

- 这个项目解决什么问题；
- Linear、GitHub、两个 Skills 和人分别承担什么角色；
- 用户如何安装和开始使用；
- 哪些操作需要人工确认；
- 如何判断结果是否真实完成；
- 当前有哪些限制和未完成验证。

避免把 README 写成内部交接记录、调试日志、考核材料或只对作者有意义的版本说明。

新增案例建议使用以下结构：

```text
项目背景
→ 原有问题
→ Linear 结构
→ Skills 如何参与
→ GitHub 或其他证据如何核验
→ 实际结果
→ 尚未验证的边界
→ 复用方式
```

## 开发和验证

提交前运行：

```powershell
python -m pip install -r requirements-dev.txt
python -m compileall -q scripts skills tests
python -m unittest discover -s tests -p "test_*.py" -v
python scripts/build_skill_archives.py
python tests/validate_skills.py
```

修改 Skill 规则时，还需要检查：

- 是否改变触发范围；
- 是否扩大读取或写入权限；
- 是否影响 Plan ID、项目 Profile、证据规则或审查结论；
- 是否改变资源路由和自包含要求；
- 是否需要更新版本说明和测试场景。

## Pull Request 建议

- 一个 PR 只处理一个明确主题；
- 标题说明行为变化，而不是只写“update”；
- 描述中说明问题、方案、验证结果和兼容性影响；
- 文档变化列出受影响的使用路径；
- 新功能附最小使用示例；
- 规则变化附正例、反例和降级行为；
- 不通过静默默认值掩盖缺失配置或失败状态。

## 讨论优先的改动

以下改动建议先开 Issue 讨论：

- 增加新的正式事项类型；
- 改变确认或写入安全流程；
- 允许新的自动写入目标；
- 改变健康状态或证据矩阵；
- 支持新的项目管理平台；
- 改变 Profile Schema；
- 引入新的运行时依赖。

## 行为准则

请聚焦问题、证据和可复现结果。可以不同意设计，但不要发布敏感数据、攻击个人，或把未经验证的推断写成事实。
